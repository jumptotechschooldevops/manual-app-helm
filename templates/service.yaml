apiVersion: v1
kind: Service
metadata:
  name: {{ include "manual-app.fullname" . }}
  labels:
    {{- include "manual-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "manual-app.selectorLabels" . | nindent 4 }}
